name: front_detection
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1


dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  get: ^4.7.3
  camera: ^0.10.5+9
  vibration: ^3.1.3  # ← ajouter
  speech_to_text: ^7.0.0
  flutter_tts: ^4.2.0  # ← ajouter
  http: ^1.1.0           # Pour envoyer des requêtes HTTP
  #image_picker: ^0.8.7
  image: ^4.1.7     # Pour convertir l'image en bytes
  image_picker: ^1.1.2
  dotted_border: ^2.1.0
dev_dependencies:
  flutter_test:
    sdk: flutter


  flutter_lints: ^5.0.0


flutter:
  uses-material-design: true

